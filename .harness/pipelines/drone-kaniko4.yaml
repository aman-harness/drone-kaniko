name: Sample Pipeline_1676527849796
stages:
  - name: build
    spec:
      steps:
        - name: go_build
          spec:
            run: go build
          type: script
        - name: go_test
          spec:
            run: go test -v ./...
          type: script
    type: ci
version: 1
